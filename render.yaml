services:
  - type: web
    name: time-control-api
    runtime: go
    plan: free
    buildCommand: go build -ldflags="-s -w" -o api ./cmd/api
    startCommand: ./api
    envVars:
      - key: DB_URL
        sync: false
      - key: JWT_SECRET
        sync: false
    healthCheckPath: /health
